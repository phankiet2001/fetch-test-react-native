import {StyleSheet} from 'react-native';

import {Colors} from '@/const';

export const blueLineStyles = StyleSheet.create({
  blueLine: {
    backgroundColor: Colors.deepBlue,
    width: 2,
    height: 30,
    marginLeft: 7,
    marginBottom: 5,
  },
});
